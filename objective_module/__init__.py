"""Canonical DSP/objective implementation - see afpx_objective.py.

Marked as a regular package (not an implicit namespace package) so
setuptools.packages.find can discover and install it. See CHANGELOG.md.
"""
